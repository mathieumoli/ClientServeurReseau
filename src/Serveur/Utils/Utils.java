package Serveur.Utils;

/**
 * Created by user on 13/11/15.
 * Les trucs qu'on a besoin un peu partout
 */
public class Utils {
    /* port number to reach the server */
    public static final int NUM_PORT = 6969;

    /* address of the server */
    public static final byte[] addresseServeur = new byte[] {(byte)10, (byte)212, (byte)102, (byte)174};
}
